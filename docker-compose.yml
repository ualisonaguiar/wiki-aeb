version: "3.8"

services:
  wiki:
    build: .
    container_name: cti-wiki
    restart: unless-stopped
    ports:
      - 5173:5173
      - 3000:3000
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3000
