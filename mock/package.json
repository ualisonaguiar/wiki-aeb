{
  "name": "cti-wiki-mock",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "mock": "json-server --watch db.json --port 4000 --host 0.0.0.0"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}
