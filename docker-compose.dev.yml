services:
  wiki-dev:
    image: node:22
    container_name: wiki-dev
    working_dir: /usr/src/app
    ports:
      - 5173:5173
      - 3000:3000
    volumes:
      - ./.:/usr/src/app
    stdin_open: true
    tty: true
    command: sh -c "npm install && npm run dev"
